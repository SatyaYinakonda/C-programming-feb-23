//PROGRAM OF RECIPROCAL OF NUMBERS TO PRINT THEIR SUM//
#include<stdio.h>
main()
{
	int i,n;
	float sum=0;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
		{
		sum=sum+1.0/i;
		}
		printf("%.4f",sum);
	return 0;	
}
